export default ({ children }) => (
    <button
        aria-label="Button Icon"
        className="icon h-12 w-[60px] p-[9px_12px] flex items-center rounded-lg shadow-inside"
    >
        {children}
    </button>
)
